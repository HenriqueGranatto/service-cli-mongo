'use strict'

const mongoose = require('mongoose')
const Schema = mongoose.Schema

const <%- props.name %>Schema = new Schema(<%- props.schema %>, {"timestamps": {"createdAt": "created_at"}});

module.exports = mongoose.model('<%- props.name %>', <%- props.name %>Schema)
